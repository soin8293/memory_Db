"""Local-only embeddings backends.

This package intentionally avoids any network calls.

Backends:
- hash (deterministic stub; NOT semantic)

Real semantic embedder is approval-gated and will be added later.
"""
