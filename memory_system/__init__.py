"""Tracked memory tooling (local-only).

This package is intentionally lightweight and contains:
- tools
- policy
- embeddings (local-only)

Memory *data* lives in data/ (gitignored, never committed).
"""
